# commands/

Single-shot, parameterized instructions triggered explicitly by the user.

Examples: `scaffold-feature`, `add-env-var`, `add-route`.

Each file is a self-contained instruction the agent executes when invoked by name.
