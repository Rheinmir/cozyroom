apiVersion: v1
kind: PersistentVolume
metadata:
  name: k8s-data
spec:
  capacity:
    storage: 5Gi
  accessModes: [ReadWriteOnce]
  hostPath:
    path: /mnt/c/Users/olive/orca/k8s-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values: [rhein-13700hxes-4070-64-4t]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cozyroom-data
  namespace: cozyroom-k8s
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ""
  volumeName: k8s-data
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: cozyroom-k8s
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      nodeSelector:
        kubernetes.io/hostname: rhein-13700hxes-4070-64-4t
      containers:
        - name: backend
          image: 100.88.197.64:5000/cozyroom-backend:k8s
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cozyroom-secret
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: "postgres://cozyroom:$(POSTGRES_PASSWORD)@db-adapter:5432/cozyroom?sslmode=disable&default_query_exec_mode=simple_protocol"
            - name: MUSIC_PATH
              value: /music
            - name: YT_DOWNLOAD_PATH
              value: /youtube
            - name: FILMS_PATH
              value: /films
            - name: EBOOKS_PATH
              value: /ebooks
            - name: EBOOK_COVERS_DIR
              value: /data/ebook-covers
            - name: CLOAK_PROXY_URL
              value: "http://cloak-proxy:8765"
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: cozyroom-secret
                  key: OPENROUTER_API_KEY
            - name: DEEPSEEK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: cozyroom-secret
                  key: DEEPSEEK_API_KEY
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: cozyroom-secret
                  key: GEMINI_API_KEY
            - name: GEMINI_API_KEYS
              valueFrom:
                secretKeyRef:
                  name: cozyroom-secret
                  key: GEMINI_API_KEYS
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cozyroom-secret
                  key: GITHUB_TOKEN
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: music
              mountPath: /music
              readOnly: true
            - name: films
              mountPath: /films
              readOnly: true
            - name: ebooks
              mountPath: /ebooks
              readOnly: true
            - name: data
              mountPath: /data
            - name: yt-downloads
              mountPath: /youtube
      volumes:
        - name: music
          hostPath:
            path: /mnt/f/250930music
        - name: films
          hostPath:
            path: /mnt/f/Films
        - name: ebooks
          hostPath:
            path: /mnt/f/Ebooks
        - name: data
          persistentVolumeClaim:
            claimName: cozyroom-data
        - name: yt-downloads
          persistentVolumeClaim:
            claimName: yt-downloads
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cozyroom-k8s
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
